/* 使用create页面的设计风格 */
.container {
  padding: 30rpx;
  min-height: 100vh;
  background-color: #f8f9fa;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 卡片样式 - 与create页面保持一致 */
.section-card {
  width: 100%;
  background: #fff;
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.05);
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 加载状态 */
.loading-container {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #666;
  font-size: 28rpx;
}

.loading-icon {
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 30rpx;
  opacity: 0.6;
}

/* 进度条 */
.progress-container {
  margin-bottom: 20rpx;
  padding: 20rpx 0;
  width: 100%;
}

.progress-bar {
  height: 16rpx;
  background-color: #e4e8f0;
  border-radius: 10rpx;
  overflow: hidden;
}

.progress-active {
  height: 100%;
  background: linear-gradient(135deg, #2c7be5 0%, #1a56db 100%);
  border-radius: 10rpx;
  transition: width 0.3s ease;
}

.progress-text {
  display: block;
  text-align: right;
  font-size: 26rpx;
  color: #2c7be5;
  margin-top: 10rpx;
  font-weight: bold;
}

/* 问卷标题 */
.questionnaire-header {
  margin-bottom: 20rpx;
}

.title {
  font-size: 36rpx;
  font-weight: bold;
  display: block;
  margin-bottom: 15rpx;
  color: #333;
  text-align: center;
  white-space: normal;
  word-break: break-all;
  word-wrap: break-word;
}

.description {
  font-size: 28rpx;
  color: #666;
  display: block;
  line-height: 1.5;
  text-align: center;
  white-space: normal;
  word-break: break-all;
  word-wrap: break-word;
}

/* 问题内容 */

/* 模块标签样式 */
.module-tag {
  font-size: 24rpx;
  color: #1890ff;
  background-color: #e6f7ff;
  padding: 6rpx 16rpx;
  border-radius: 16rpx;
  margin-top: 10rpx;
  display: inline-block;
}
.question-content {
  flex: 1;
  overflow-y: auto;
  margin-bottom: 20rpx;
  width: 100%;
}

.question-card {
  min-height: 450rpx;
  display: flex;
  flex-direction: column;
  background: #f8fafc;
  border-radius: 15rpx;
  padding: 30rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.03);
  border: 1rpx solid #edf2f7;
  width: 100%;
  box-sizing: border-box;
}

.question-header {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
  width: 100%;
  justify-content: center;
}

.question-index {
  font-size: 36rpx;
  font-weight: bold;
  color: #2c7be5;
  margin-left: 15rpx;
}

.small-edit-btn {
  font-size: 24rpx;
  padding: 8rpx 16rpx;
  margin: 10rpx auto 20rpx;
  background-color: #2c7be5;
  color: white;
  border-radius: 8rpx;
  min-width: auto;
  display: block;
}

.question-type {
  font-size: 28rpx;
  color: #718096;
}

.question-title {
  font-size: 32rpx;
  margin-bottom: 30rpx;
  display: block;
  color: #333;
  line-height: 1.5;
  text-align: center;
  font-weight: 500;
}

/* 选项 - 使用create页面的选项样式 */
.options {
  margin-top: 20rpx;
  width: 100%;
  padding: 0;
}

.option-item {
  font-size: 28rpx;
  color: #000000;
  margin-bottom: 0;
  padding: 28rpx 30rpx;
  background: #fff;
  border-bottom: 1rpx solid #f0f2f5;
  display: flex;
  align-items: center;
  width: 100%;
  box-sizing: border-box;
  padding: 20rpx 0;
}

.radio {
  width: 40rpx;
  height: 40rpx;
  border: 2rpx solid #999;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-right: 20rpx;
}

.radio.checked {
  border-color: #1890ff;
}

.radio .dot {
  width: 20rpx;
  height: 20rpx;
}

/* Checkbox样式已合并到上方定义 */

/* 选项选中状态样式 */
.option-item.selected {
  background-color: #e6f7ff; /* 保留浅蓝色背景 */
  color: #1890ff;
  border-radius: 8rpx; /* 改为圆角矩形更美观 */
}

.radio.checked .dot {
  background-color: #1890ff;
}

/* 多选题复选框样式已合并到上方定义 */

.option-text-container {
  flex: 1;
  overflow-x: auto;
  white-space: nowrap;
  padding-left: 20rpx;
}

.option-letter-container {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #e6f7ff;
  border-radius: 50%;
  flex-shrink: 0;
  position: sticky;
  left: 0;
  z-index: 1;
  background-color: #fff;
}

.option-letter {
  font-size: 30rpx;
  font-weight: bold;
  color: #1890ff;
}

/* 选项字母文本框样式 - 与create页面保持一致 */
.option-letter-input {
  width: 60rpx;
  height: 80rpx;
  font-size: 28rpx;
  font-weight: bold;
  color: #1890ff;
  border: 1rpx solid #d9d9d9;
  border-radius: 8rpx;
  text-align: center;
  background-color: #f8f9fb;
  margin-right: 15rpx;
}

.option-text {
  flex: 1;
  font-size: 28rpx;
  color: #333;
}

/* 多选题禁用样式 */
.disabled-option {
  opacity: 0.6;
  pointer-events: none;
}

/* 问答题输入区域 - 关键修改：移除图标，只显示文字提示 */
.text-input-placeholder {
  width: 100%;
  height: 200rpx;
  background-color: #f8fafc;
  border-radius: 15rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 20rpx;
  border: 1rpx dashed #d9dde3;
  color: #718096;
  font-size: 28rpx;
  padding: 30rpx;
  box-sizing: border-box;
}

/* 问答题回答内容显示区域 */
.text-answer-container {
  width: 100%;
  min-height: 200rpx;
  background-color: #f8fafc;
  border-radius: 15rpx;
  margin-top: 20rpx;
  padding: 30rpx;
  box-sizing: border-box;
  border: 1rpx solid #e4e8f0;
}

.text-answer {
  width: 100%;
  font-size: 28rpx;
  color: #333;
  line-height: 1.5;
  white-space: pre-wrap;
  word-break: break-all;
}

/* 填空题回答区域样式 */
.blanks-answer-container {
  margin-top: 20rpx;
  width: 100%;
}

.blank-answer-item {
  display: flex;
  align-items: center;
  padding: 15rpx 0;
  border-bottom: 1rpx solid #f0f2f5;
}

.blank-label {
  width: 120rpx;
  font-size: 28rpx;
  color: #666;
  text-align: right;
  margin-right: 20rpx;
  height: 80rpx;
  line-height: 80rpx;
}

.blank-content {
  flex: 1;
  font-size: 28rpx;
  color: #333;
  padding: 20rpx;
  background-color: #f8fafc;
  border-radius: 8rpx;
  min-height: 80rpx;
  line-height: 80rpx;
  box-sizing: border-box;
}

/* 填空题输入框样式 - 与编辑问题页面保持一致 */
.blank-input {
  flex: 1;
  font-size: 28rpx;
  color: #333;
  padding: 0 20rpx;
  background-color: #ffffff;
  border: 1rpx solid #d9d9d9;
  border-radius: 8rpx;
  height: 80rpx;
  line-height: 80rpx;
  box-sizing: border-box;
  margin-right: 15rpx;
}

/* 画图区域 - 确保示例图片有具体高度 */
.draw-area {
  width: 100%;
  flex: 1;
  background-color: #f8fafc;
  border-radius: 15rpx;
  margin-top: 20rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border: 1rpx dashed #d9dde3;
  position: relative;
  min-height: 400rpx;
}

/* 画板样式 */
.draw-canvas {
  width: 100%;
  height: 400rpx;
}

/* 画板工具栏 */
.draw-toolbar {
  display: flex;
  flex-direction: column;
  width: 100%;
  padding: 20rpx;
  background-color: #fff;
  border-radius: 10rpx;
  margin-top: 20rpx;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.05);
}

.tool-group {
  display: flex;
  align-items: center;
  margin-bottom: 15rpx;
}

.color-btn {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  margin: 0 10rpx;
  border: 2rpx solid transparent;
}

.color-btn.active {
  border-color: #1890ff;
}

.line-width-btn {
  width: 40rpx;
  height: 10rpx;
  background-color: #333;
  margin: 0 10rpx;
  border-radius: 5rpx;
}

.line-width-btn.active {
  background-color: #1890ff;
}

.action-btn {
  padding: 8rpx 20rpx;
  background-color: #f8f8f8;
  border: 1rpx solid #d9d9d9;
  border-radius: 30rpx;
  font-size: 24rpx;
  margin: 0 10rpx;
}

.undo-btn {
  background-color: #e6f7ff;
  border-color: #91d5ff;
  color: #1890ff;
}

.clear-btn {
  background-color: #fff1f0;
  border-color: #ffa39e;
  color: #f5222d;
}

/* 示例图片样式 - 确保高度 */
.draw-area image {
  width: 100%;
  height: 400rpx;
  border-radius: 10rpx;
}

.image-hint {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  background: rgba(0, 0, 0, 0.6);
  color: white;
  padding: 8rpx 20rpx;
  border-radius: 30rpx;
  font-size: 24rpx;
  z-index: 10;
}

.no-image {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: #718096;
  font-size: 28rpx;
}

.draw-icon {
  width: 100rpx;
  height: 100rpx;
}

/* 按钮行样式已合并到上方定义 */

/* 按钮大小相同的行样式 */
.same-size-buttons {
  display: flex;
  justify-content: space-between;
  width: 100%;
}

.same-size-buttons button {
  flex: 1;
  margin: 0 10rpx;
}

/* 结束预览和选题按钮行样式 */
.finish-select {
  justify-content: space-around;
}

/* 按钮通用样式 */
.btn-prev, .btn-next, .btn-select, .btn-finish, .btn-save, .btn-submit {
  margin: 0 10rpx;
  padding: 0 40rpx;
  height: 88rpx;
  line-height: 88rpx;
  font-size: 32rpx;
  border-radius: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 180rpx;
}

/* 上一题按钮样式 */
.btn-prev {
  background-color: #e6f7ff;
  color: #1890ff;
  border: 1rpx solid #91d5ff;
}

/* 下一题按钮样式 */
.btn-next {
  background-color: #1890ff;
  color: #fff;
  border: 1rpx solid #1890ff;
}

/* 选题按钮样式 */
.btn-select {
  background-color: #4c95f5;
  color: #fff;
  border: 1rpx solid #4c95f5;
}

/* 保持答案按钮样式 */
.btn-save {
  background-color: #e6f7ff;
  color: #1890ff;
  border: 1rpx solid #91d5ff;
}

/* 提交问卷按钮样式 */
.btn-submit {
  background-color: #ff4d4f;
  color: #fff;
  border: 1rpx solid #ff4d4f;
}

/* 按钮行样式 */
.row-buttons {
  display: flex;
  justify-content: center;
  margin-bottom: 20rpx;
  width: 100%;
  flex-wrap: wrap;
}

/* 结束预览按钮样式 */
.btn-finish {
  background-color: #ff4d4f;
  color: #fff;
  border: none;
}

/* 选题按钮样式已合并到上方定义 */

/* 选题弹窗遮罩 */
.question-selector-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  z-index: 999;
}

/* 选题弹窗容器 */
.question-selector {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  background: #fff;
  border-radius: 20rpx 20rpx 0 0;
  padding: 30rpx;
  box-sizing: border-box;
  z-index: 1000;
  max-height: 70vh;
  overflow-y: auto;
}

/* 弹窗标题 */
.selector-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 30rpx;
  text-align: center;
}

/* 题目列表 */
.question-list {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

/* 题目项 */
.question-item {
  width: 80rpx;
  height: 80rpx;
  background: #f8fafc;
  border: 1rpx solid #e2e8f0;
  border-radius: 10rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 20rpx;
  font-size: 28rpx;
  color: #333;
}

/* 当前选中题目 */
.question-item.current {
  background: #2c7be5;
  color: #fff;
  border-color: #2c7be5;
  font-weight: bold;
}

/* 按钮区域 */
.buttons {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 20rpx 0;
  border-top: 1rpx solid #eee;
  margin-top: auto;
  width: 100%;
}

.row-buttons {
  display: flex;
  gap: 20rpx;
  width: 100%;
}

.btn-prev, .btn-next, .btn-finish {
  height: 90rpx;
  line-height: 90rpx;
  font-size: 32rpx;
  font-weight: bold;
  border-radius: 50rpx;
  transition: all 0.3s;
  text-align: center;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.btn-prev, .btn-next {
  flex: 1;
}

.btn-finish {
  width: 100%;
}

.btn-prev {
  background: #fff;
  color: #2c7be5;
  border: 2rpx solid #2c7be5;
}

.btn-next {
  background: linear-gradient(135deg, #2c7be5 0%, #1a56db 100%);
  color: #fff;
}

.btn-finish {
  background: linear-gradient(135deg, #4CAF50 0%, #2E7D32 100%);
  color: #fff;
}

.btn-prev[disabled], .btn-next[disabled] {
  opacity: 0.6;
}

.btn-prev:active {
  transform: translateY(4rpx);
  box-shadow: 0 2rpx 6rpx rgba(44, 123, 229, 0.3);
}

.btn-next:active {
  transform: translateY(4rpx);
  box-shadow: 0 2rpx 6rpx rgba(26, 86, 219, 0.4);
}

.btn-finish:active {
  transform: translateY(4rpx);
  box-shadow: 0 2rpx 6rpx rgba(46, 125, 50, 0.4);
}

/* 画钟表题提示句样式 */
.clock-prompt-container {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #fff;
  border-radius: 15rpx;
  border: 1rpx solid #edf2f7;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.03);
  width: 100%;
  box-sizing: border-box;
}

.prompt-title {
  font-size: 28rpx;
  color: #4a5568;
  font-weight: bold;
  margin-right: 10rpx;
}

.prompt-content {
  font-size: 28rpx;
  color: #2d3748;
  line-height: 1.5;
}

.prompt-options {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 15rpx;
}

.option-tag {
  background: #ebf5ff;
  color: #2c7be5;
  padding: 6rpx 20rpx;
  border-radius: 30rpx;
  font-size: 24rpx;
  border: 1rpx solid #c0deff;
}

/* 连线题提示句样式 */
.connect-prompt-container {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #fff;
  border-radius: 15rpx;
  border: 1rpx solid #edf2f7;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.03);
  width: 100%;
  box-sizing: border-box;
}

/* 画图案和画立体图题提示句样式 */
.draw-prompt-container {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #fff;
  border-radius: 15rpx;
  border: 1rpx solid #edf2f7;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.03);
  width: 100%;
  box-sizing: border-box;
}

/* 钟表题选项标签样式 */
.prompt-options {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 16rpx;
}

.option-tag {
  padding: 8rpx 16rpx;
  background-color: #e6f7ff;
  color: #1890ff;
  border-radius: 16rpx;
  font-size: 24rpx;
  border: 1rpx solid #91d5ff;
}

/* 画板内容预览 */
.drawing-preview-container {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #f8fafc;
  border-radius: 10rpx;
  border: 1rpx dashed #cbd5e0;
  width: 100%;
  box-sizing: border-box;
}

.preview-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #4a5568;
  margin-bottom: 10rpx;
  display: block;
}

.drawing-preview {
  width: 100%;
  height: 300rpx;
  border-radius: 8rpx;
  background-color: #fff;
  border: 1rpx solid #e2e8f0;
}

.empty-drawing {
  font-size: 26rpx;
  color: #a0aec0;
  text-align: center;
  padding: 40rpx 0;
  display: block;
}

/* 连线题例图容器 */
.example-image-container {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #f8fafc;
  border-radius: 10rpx;
  border: 1rpx dashed #cbd5e0;
  width: 100%;
  box-sizing: box;
}

/* 连线题例图样式 */
.example-image {
  width: 100%;
  height: 300rpx;
  border-radius: 8rpx;
  background-color: #fff;
  border: 1rpx solid #e2e8f0;
}

/* 勾选题矩阵样式 - 与管理员页面保持一致 */
.matrix-edit-container {
  position: relative;
  width: 100%;
  overflow: hidden; /* 限制内部滚动 */
  margin: 20rpx auto;
  border: 1rpx solid #e8e8e8;
  border-radius: 8rpx;
  display: flex;
  flex-direction: column;
}

/* 解决表头错位问题 - 确保所有单元格宽度一致 */
.matrix-header-scroll,
.matrix-body-scroll {
  width: 100%;
}

.matrix-body-scroll {
  flex: 1;
}

/* 确保滚动同步 */
.matrix-header-scroll::-webkit-scrollbar,
.matrix-body-scroll::-webkit-scrollbar,
.matrix-cells-scroll::-webkit-scrollbar {
  height: 6rpx;
  width: 6rpx;
}

.matrix-header-scroll::-webkit-scrollbar-thumb,
.matrix-body-scroll::-webkit-scrollbar-thumb,
.matrix-cells-scroll::-webkit-scrollbar-thumb {
  background: #c1c1c1;
  border-radius: 3rpx;
}

.matrix-header-scroll::-webkit-scrollbar-track,
.matrix-body-scroll::-webkit-scrollbar-track,
.matrix-cells-scroll::-webkit-scrollbar-track {
  background: #f1f1f1;
  border-radius: 3rpx;
}

/* 矩阵头部滚动区域 - 与单元格区域宽度一致 */
.matrix-header-scroll {
  width: calc(100% - 180rpx);
  overflow-x: auto;
  white-space: nowrap;
  will-change: transform;
  transform: translateZ(0);
  position: relative;
  margin-left: 180rpx;
}

/* 确保标题行宽度自适应内容 */
.matrix-header {
  display: flex;
  background-color: #f8f9fb;
  border-bottom: 1rpx solid #f0f2f5;
  width: max-content;
}

/* 矩阵主体滚动区域 */
.matrix-body-scroll {
  flex: 1;
  overflow-y: auto;
  will-change: transform;
  transform: translateZ(0);
}

/* 矩阵单元格滚动区域 */
.matrix-cells-scroll {
  flex: 1;
  overflow-x: auto;
  white-space: nowrap;
  will-change: transform;
  transform: translateZ(0);
  /* 关键修改：确保单元格区域与表头区域宽度一致 */
  width: calc(100% - 180rpx);
}

/* 矩阵标题行 */
.matrix-header {
  display: flex;
  background-color: #f8f9fb;
  border-bottom: 1rpx solid #f0f2f5;
}

/* 列标题单元格 - 与矩阵单元格宽度严格一致 */
.header-cell {
  flex: 0 0 180rpx; /* 固定宽度 */
  height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #4a5568;
  font-weight: 500;
  border-right: 1rpx solid #f0f2f5;
  padding: 0 10rpx;
  background-color: #f8f9fb;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  box-sizing: border-box;
}

/* 矩阵单元格样式已合并到上方定义 */

/* 左上角固定单元格 */
.fixed-corner {
  position: absolute;
  top: 0;
  left: 0;
  width: 180rpx;
  height: 80rpx;
  background-color: #f1f5f9;
  border-right: 1rpx solid #f0f2f5;
  border-bottom: 1rpx solid #f0f2f5;
  z-index: 3;
}

/* 行容器 */
.matrix-row {
  display: flex;
  border-bottom: 1rpx solid #f0f2f5;
}

/* 行标题 */
.row-header {
  width: 180rpx;
  height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #4a5568;
  font-weight: 500;
  background-color: #f8f9fb;
  border-right: 1rpx solid #f0f2f5;
  position: sticky;
  left: 0;
  z-index: 2;
}

/* 单元格容器 */
.matrix-row-cells {
  display: flex;
}

/* 矩阵单元格样式已合并到上方定义 */

/* 矩阵单元格样式 - 解决复选框错位问题 */
.matrix-cell {
  flex: 0 0 180rpx; /* 固定宽度，与表头单元格一致 */
  height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-right: 1rpx solid #f0f2f5;
  box-sizing: border-box;
  background-color: #fff;
}

/* 复选框样式 */
.checkbox {
  width: 40rpx;
  height: 40rpx;
  border: 2rpx solid #cbd5e0;
  border-radius: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
}

.checkbox.checked {
  border-color: #1890ff;
  background-color: #e6f7ff;
}

.checkbox.checked::after {
  content: "";
  width: 20rpx;
  height: 10rpx;
  border-left: 3rpx solid #1890ff;
  border-bottom: 3rpx solid #1890ff;
  transform: rotate(-45deg);
}

/* 图片上传容器 */
.upload-container {
  padding: 20rpx 30rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  background-color: #f8f9fb;
  border-bottom: 1rpx solid #f0f2f5;
  margin: 10rpx 0;
  border-radius: 16rpx;
  border: 1rpx solid #e2e8f0;
  width: 100%;
  box-sizing: border-box;
}

/* 已上传图片样式 */
.uploaded-image {
  width: 100%;
  height: 400rpx;
  object-fit: contain;
  border-radius: 12rpx;
  margin-top: 20rpx;
}

/* 无图片占位样式已合并到上方定义 */

/* 区域标题样式 */
.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 30rpx 20rpx;
  background-color: #fafafa;
  border-bottom: 1rpx solid #f0f2f5;
  margin: 10rpx 0;
}

.section-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1a1a1a;
}

/* 问答题输入框样式 - 与编辑问题页面保持一致 */
.text-input {
  width: 100%;
  min-height: 200rpx;
  background-color: #fff;
  border-radius: 15rpx;
  margin-top: 20rpx;
  padding: 30rpx;
  box-sizing: border-box;
  border: 1rpx solid #d9d9d9;
  font-size: 28rpx;
  color: #333;
  line-height: 1.5;
  resize: none;
}

.text-input:focus {
  border-color: #1890ff;
  outline: none;
}